## Archived
Этот репозиторий архивирован. Функционал включён в [surgical-research-platform-mvp](https://github.com/Serg2206/surgical-research-platform-mvp).

---

# Surgery-Onko-SSV

Платформенный многомодульный научно-практический оригинальный репозиторий SSV.

## ⚠️ ВАЖНО: Статус Данных

**Текущая версия использует синтетические (демонстрационные) данные для разработки и тестирования.**

- Данные в `data/gastrectomy_patients.json` созданы искусственно и **не являются реальными клиническими данными**
- ML-модели обучены на синтетических данных для демонстрации функционала системы
- Для использования в реальных медицинских исследованиях требуется интеграция с клинической базой данных
- Планируется: интеграция с защищенными клиническими источниками при получении соответствующих разрешений

## О проекте

Создан в качестве платформы для работы с молекулярными данными и анализа их в контексте хирургии и онкологии. Проект включает в себя следующие функции:

* Анализ молекулярных данных
* Предобработка и анализ данных
* Работа с обученными моделями

## Технологии

* JavaScript (Node.js)
* TensorFlow.js
* pandas-js
* Express.js (API)
* helmet (безопасность)
* Jest (тестирование)

## Структура проекта

```
Platform-Surgery-Onko-SSV/
├── src/
│   └── index.js              # API-сервер с аутентификацией и безопасностью
├── scripts/
│   ├── train_gastrectomy_model.js       # Обучение модели прогнозирования
│   └── cross_validate_gastrectomy.js    # Кросс-валидация модели
├── data/
│   └── gastrectomy_patients.json        # Синтетические данные (демо)
├── tests/
│   └── app.test.js           # Тесты приложения
├── doc/
│   └── research_methodology_guide.md    # Методология научных исследований
├── .github/
│   └── workflows/
│       └── security.yml      # CI/CD с проверками безопасности
└── package.json
```

## Безопасность

Приложение включает:

- **helmet** для настройки HTTP-заголовков безопасности
- **Аутентификацию** через API-токены
- **Валидацию входных данных** для всех API-эндпоинтов
- Автоматические проверки безопасности через GitHub Actions

## Установка

```bash
# Клонирование репозитория
git clone https://github.com/Serg2206/Platform-Surgery-Onko-SSV.git
cd Platform-Surgery-Onko-SSV

# Установка зависимостей
npm install

# Запуск сервера (по умолчанию порт 3000)
npm start

# Запуск тестов
npm test
```

*   **Запуск в Docker**
    ```bash
    docker build -t surgery-onko-ssv .
    docker run -p 3000:3000 --env API_TOKEN=your_token surgery-onko-ssv
    ```

### Переменные окружения

Создайте файл `.env` в корне проекта:

```
API_TOKEN=your_secure_api_token_here
PORT=3000
```

⚠️ **Важно**: Никогда не коммитьте файл `.env` с реальными токенами!

## Использование API

### Получение предсказаний модели

```bash
curl -X POST http://localhost:3000/api/v1/predict \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -d '{
    "age": 65,
    "sex": "M",
    "bmi": 24.5,
    "tumor_stage": "IIIA",
    "surgery_type": "open"
  }'
```

## Обучение модели

```bash
# Обучение модели прогнозирования гастрэктомии
node scripts/train_gastrectomy_model.js

# Кросс-валидация модели
node scripts/cross_validate_gastrectomy.js
```

## Научная методология

Детальное описание методологии исследований, планирования экспериментов и работы с данными:

📚 [Руководство по методологии исследований](doc/research_methodology_guide.md)

## Лицензия

MIT License

## Контакты

Serg2206 - GitHub: [@Serg2206](https://github.com/Serg2206)

---

**Disclaimer**: Этот проект предназначен для научно-исследовательских целей. Не используйте его для принятия медицинских решений без консультации с квалифицированными специалистами.
